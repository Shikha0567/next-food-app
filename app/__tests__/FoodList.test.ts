/* import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom";

describe('FoodList Unit Tests',()=>{
it("should render 'Food Delivery Restaurants in Bengaluru' heading", () => {
  render(<FoodList />);
  const heading = screen.getByText(/Food Delivery Restaurants in Bengaluru/i);
  expect(heading).toBeInTheDocument();
});
it("should render 21 cards with 'food-card' test id", () => {
  render(<FoodList />);
  const foodList = screen.getAllByTestId("food-card");
  expect(foodList).toHaveLength(21);
});
})
 */
